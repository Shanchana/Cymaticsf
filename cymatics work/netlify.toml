[build]
  base = "cymatics work"
  command = "apt-get update && apt-get install -y python3-dev libpq-dev && pip install -r requirements.txt && python manage.py collectstatic --noinput"
  publish = "staticfiles"